h1 = t('.title')
hr
.card
  .card-header
    strong = "#{t('activerecord.attributes.map_area.name')}:"
  .card-body
    = @map_area.name
.card
  .card-header
    strong = "#{t('.mob.name')}:"
  .card-body
    = render 'shared/list', list: @map_area.mobs

hr
.btn-toolbar
  - if admin_user?
    .btn-group.mr-2
      = link_to t('simple_form.btn.edit'), edit_map_area_path(@map_area), class: "btn btn-primary"
  .btn-group.mr-2
    = link_to t('simple_form.btn.back'), map_areas_path, class: "btn btn-secondary"
hr